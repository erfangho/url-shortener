name: CI
on:
  push:
    branches:
      - main
  pull_requests:
    types:
      - opened
      - synchronize

env:
  GO_VERSION: '1.27'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from GitHub Actions"
      - run: echo "Go version:$GO_VERSION"