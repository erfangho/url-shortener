meta {
  name: Get URL Info
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/urls/:shortCode
  body: none
  auth: none
}

params:path {
  shortCode: e086ee
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  })
  
  test("Response should have data", function() {
    expect(res.body.data).to.be.an("object");
  })
}
