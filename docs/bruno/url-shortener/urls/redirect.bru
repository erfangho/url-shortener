meta {
  name: Redirect to Original URL
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/u/:shortcode
  body: none
  auth: none
}

params:path {
  shortcode: e086ee
}

tests {
  test("Status should be 301", function() {
    expect(res.getStatus()).to.equal(301);
  })
}

settings {
  encodeUrl: false
  timeout: 0
}
